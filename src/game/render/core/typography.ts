/** One font contract for every Pixi label and generated text texture. */
export const GAME_FONT_FAMILY = "Be Vietnam Pro"
export const GAME_FONT_STACK = '"Be Vietnam Pro", system-ui, sans-serif'

export const FONT_SAMPLES = [
  `900 40px "${GAME_FONT_FAMILY}"`,
  `800 30px "${GAME_FONT_FAMILY}"`,
  `700 24px "${GAME_FONT_FAMILY}"`,
  `600 16px "${GAME_FONT_FAMILY}"`,
  `400 12px "${GAME_FONT_FAMILY}"`,
]
